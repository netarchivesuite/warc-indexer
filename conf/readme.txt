config3.xml:

Default configuration for the warc-indexer. It must be given as an argument to the warc-indexer when starting.
The suffix '3' indicates it is compatible with the warc-indexer version 3.x.x  